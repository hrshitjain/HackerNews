import { useMemo } from "react";
import { fetchNewsIds } from "../services/APIService";

export const fetchLatestNews = (props) => {
    
}

export const fetchIds = () => {
    const newsIds = fetchNewsIds();
    return newsIds;
}

export const HeadLinesViewModel = () => {


    
}